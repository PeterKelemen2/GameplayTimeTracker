<Application x:Class="GameplayTimeTracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GameplayTimeTracker"
             xmlns:button="clr-namespace:GameplayTimeTracker.Converters.Button"
             xmlns:converters="clr-namespace:GameplayTimeTracker.Converters"
             xmlns:viewModels="clr-namespace:GameplayTimeTracker.ViewModels">

    <Application.Resources>
        <button:HoverColorConverter x:Key="HoverColorConverter" />
        <button:PressedColorConverter x:Key="PressedColorConverter" />
        <converters:HexToBrushConverter x:Key="HexToBrushConverter"/>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                SnapsToDevicePixels="True">
                            <!-- ContentPresenter allows text, icon, or both -->
                            <ContentPresenter x:Name="contentPresenter"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Background, Converter={StaticResource HoverColorConverter}}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background"
                                        Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Background, Converter={StaticResource PressedColorConverter}}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FFCCCCCC" />
                                <Setter Property="Foreground" Value="#FF666666" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Positive (Green) -->
        <Style x:Key="PositiveButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonPositive, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
            <Setter Property="BorderBrush"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonPositive, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
            <Setter Property="Foreground"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonPositiveFont, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
        </Style>

        <!-- Negative (Red) -->
        <Style x:Key="NegativeButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonNegative, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
            <Setter Property="BorderBrush"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonNegative, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
            <Setter Property="Foreground"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonNegativeFont, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
        </Style>

        <!-- Neutral (Blue) -->
        <Style x:Key="NeutralButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButton, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
            <Setter Property="BorderBrush"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButton, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
            <Setter Property="Foreground"
                    Value="{Binding DataContext.(viewModels:MainWindowViewModel.CurrentTheme).CButtonFont, 
                            RelativeSource={RelativeSource AncestorType=Window}, 
                            Converter={StaticResource HexToBrushConverter}}" />
        </Style>

    </Application.Resources>
</Application>